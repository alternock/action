yaml
name: Hello World

on:
 push:
   branches:[main]

jobs:

  hello:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Hello World
      run: echo "Hello World!"
